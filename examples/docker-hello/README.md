# Docker Hello Example

This is a minimal Dockerized web app to demonstrate Docker is working.

Quick start:
- Build: `docker build -t docker-hello .`
- Run: `docker run -p 8080:8080 docker-hello`
- Open: http://localhost:8080

With Docker Compose:
- `docker compose up --build`
- Open: http://localhost:8080
